const arr = [1, 2, 3];
console.log(arr.map((iterable, callback,concurrency,)=> {
    // console.log(iterable, 'iterates');
    // console.log(callback, 'callback');
    console.log(concurrency, 'concurrency');
 
}))