import buffer from 'buffer';

//logging the maximum length of a buffer
console.log(buffer.constants.MAX_LENGTH);
//logging the maximum length of a buffer using the kMaxLength property
console.log(buffer.kMaxLength);